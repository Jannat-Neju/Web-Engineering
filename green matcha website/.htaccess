Options +FollowSymLinks
RewriteEngine On

# If the request is not a file or folder, rewrite it
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Remove .php from URL
RewriteRule ^([^/]+)/?$ $1.php [L]

# Optional: make home.php the default page
DirectoryIndex home.php
